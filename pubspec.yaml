name: flutter_test_application
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  desktop_drop: ^0.7.0
  file_picker: ^10.3.8
  ffmpeg_wasm: ^1.0.3
  path_provider: ^2.1.5
  cross_file: ^0.3.5+1
  gap: ^3.0.1
  url_launcher: ^6.3.2
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  open_file: ^3.5.11
  media_kit: ^1.2.6
  media_kit_video: ^2.0.1
  media_kit_libs_video: ^1.0.7
  # ffmpeg_kit_flutter_full_gpl: ^6.0.3  # Disabled - package discontinued and not available
  
  # Analytics dependencies
  device_info_plus: ^10.1.2
  http: ^1.2.2
  uuid: ^4.5.1
  shared_preferences: ^2.3.3
  connectivity_plus: ^6.1.1
  crypto: ^3.0.3
  package_info_plus: ^8.1.2
  flutter_dotenv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
  
  assets:
    - .env
    - assets/bin/linux/
    - assets/bin/windows/
